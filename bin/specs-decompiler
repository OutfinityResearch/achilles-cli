#!/bin/bash

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"

if [[ -z "$1" ]]; then
  echo "Usage: specs-decompiler <path-to-source-file>"
  exit 1
fi

node "$SCRIPT_DIR/../src/specs-decompiler.js" "$@"
