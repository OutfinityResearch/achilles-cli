#!/bin/bash

# Get the directory of the script
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"

# Run the achilles-codeGenerator
node "$SCRIPT_DIR/../src/achilles-codeGenerator.js"
